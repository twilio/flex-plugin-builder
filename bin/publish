#!/bin/bash

set -e

if [ -z "$TRAVIS_TAG" ] ; then
  echo 'Publish must happen on a tagged release'
  exit 1
fi

if [ "${TRAVIS_EVENT_TYPE}" == "cron" ] ; then
  echo 'skipping publishing on nightly';
  exit 0
fi

node_modules/.bin/lerna-travis-publish
