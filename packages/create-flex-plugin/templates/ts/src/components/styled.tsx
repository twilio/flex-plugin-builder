import { default as styled } from 'react-emotion';

export const Div = styled('div')`
    padding: 10px;
    margin: 0px;
    color: #fff;
    background: #000;
`;

export const Accented = styled('i')`
    color: red;
    cursor: pointer;
    float: right;
`;
