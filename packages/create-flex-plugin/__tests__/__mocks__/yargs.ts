const yargs = () => ({
  alias: () => {
    // no-op
  },
  usage: (msg: string, description: string, builder: () => void) => {
    // no-op
  },
  parse: () => {
    // no-op
  },
});

export default yargs;
