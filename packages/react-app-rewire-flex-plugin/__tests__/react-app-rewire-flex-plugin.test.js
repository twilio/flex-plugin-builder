'use strict';
const fs = require('fs');
const reactAppRewireFlexPlugin = require('..');

jest.mock('fs');

describe('react-app-rewire-flex-plugin', () => {
    it('needs tests', () => {
        expect(true).toBeTruthy();
    });
});
