name: Nightly E2E
on:
  schedule:
    - cron:  '0 4 * * *'    # At 4:00 UTC (09:30 IST) every day

jobs:
  scheduled-e2e-node16:
    uses: ./e2e.yaml
    with:
      secrets:
        BRANCH: main
        PACKAGE_VERSION: latest
        FLEX_UI_VERSION: latest