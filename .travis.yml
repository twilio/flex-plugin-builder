language: node_js

node_js:
  - '8'

before_script:
  - sudo apt-get install libsecret-1-dev -y
  - npm install

script:
  - npm run lint
  - npm run test

branches:
  except:
  - /^v\d+\.\d+\.\d+$/
  only:
  - master
  - dev
